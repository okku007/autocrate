// AutocrateCore — pure matching logic, cache, and pipeline.
// Headlessly buildable/testable via `swift test`. The macOS app target
// (App/Theme/Music/UI) lives in the Xcode project and imports this module.
import Foundation
